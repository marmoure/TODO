# description
This is a todo meant to show me what to do next and what i should be expecting to do in the future.

it should be a static app with a simple UI that can be run on github pages.
if I need to update I will update the db on my pc then push to github pages again.

# the TODOs
this app is not a simple todo app with tasks it needs to host bigger lists with infinit sub lists of info and updates about subjects

the todo needs to have a dealine, timeline of when things are expected to move along, and assoiated costs 

here's an example:
1. house rent
    a. Get the owner contact (0 dzd), deadline (01/01/2027), estimated time 5 days
    b. Call the owner (0 dzd), deadline (01/01/2027), should take 1 day
    c. draft the lease contract (0 dzd), deadline (01/01/2027), should take half a day
    d. Pay the fees (130000 * 12 dzd), deadline (01/01/2027), takes a full day

in the example above you can see the bigger subject then the sub tasks, each with it's deadline, estimated time, and costs, and the project itself should have a deadline


I should be able to add notes to each task, and if a task is delayed the whole project should be delayed too, it needs to be dynamic in that sense. and it should show 2 times expected time line and deadline and costs. 